export default class ImageSet {

  constructor(small, medium, large) {
    this.small = small;
    this.medium = medium;
    this.large = large;
  }

  // Getter that returns an image not found if it is null or something
}
